# window numbers from 1
screen 1
bind c screen 1
bind ^c screen 1

bind w windowlist -b
bind ^w windowlist -b

ignorecase on
altscreen on
autodetach on
startup_message off
vbell off
silencewait 15
defscrollback 5000
